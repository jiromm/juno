<?php

$this->layout()->breadcrumb = $this->breadcrumb([
	['Points of Sale']
]);
$this->layout()->actions = '<li><a href="' . $this->url('point-of-sale/add') . '" class="btn btn-primary">Add Point of Sale</a></li>';
$this->headTitle('Points of Sale');
$this->layout()->pageTitle = 'Points of Sale';

/**
 * @var \Config\Entity\PointOfSale[]|\ArrayObject $data
 */
$data = $this->data;

?>
<div class="row">
	<div class="col-xs-12">
		<table class="table table-bordered table-condensed table-striped table-hover dataTables">
			<thead>
			<tr>
				<th>Name</th>
				<th>Products</th>
				<th class="no-sort"></th>
			</tr>
			</thead>
			<tbody>
			<?php if ($this->data->count()) { ?>
				<?php foreach ($data as $item) { ?>
					<tr>
						<td><?php echo $item->getName(); ?></td>
						<td>xxx</td>
						<td class="text-right">
							<div class="btn-group text-left">
								<a href="<?php echo $this->url('point-of-sale/manage', ['id' => $item->getId()]); ?>" class="btn btn-xs btn-primary">Manage</a>
								<button type="button" class="btn btn-xs btn-primary dropdown-toggle" data-toggle="dropdown">
									<span class="caret"></span>
									<span class="sr-only">Toggle Dropdown</span>
								</button>
								<ul class="dropdown-menu" role="menu">
									<li><a href="<?php echo $this->url('point-of-sale/delete', ['id' => $item->getId()]); ?>">Delete</a></li>
								</ul>
							</div>
						</td>
					</tr>
				<?php } ?>
			<?php } else { ?>
				<tr>
					<td>
						No points of sale found
						<a href="<?php echo $this->url('point-of-sale/add'); ?>">Add Point of Sale</a>
					</td>
					<td></td>
					<td></td>
				</tr>
			<?php } ?>
			</tbody>
		</table>
	</div>
</div>
